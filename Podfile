# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OfflineMapDownload in swift' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod 'Mapbox-iOS-SDK'
  # Pods for OfflineMapDownload in swift

  target 'OfflineMapDownload in swiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OfflineMapDownload in swiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
